[package]
name = "bootconf"
version = "0.2.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
nix = { version = "0.27", features = ["hostname", "user", "fs"] }

[dev-dependencies]
tempfile = "3.10"
